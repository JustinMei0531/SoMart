import "package:flutter/material.dart";

class LoginOptionsPage extends StatelessWidget {
  LoginOptionsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Padding(
        padding: const EdgeInsets.all(10.0),
        child: Column(
          children: <Widget>[
            Align(),
          ],
        ),
      ),
    );
  }
}

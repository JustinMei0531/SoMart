import "package:flutter/material.dart";
import "package:get/get.dart";

class BrandsPage extends StatelessWidget {
  BrandsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Brand page"),
    );
  }
}
